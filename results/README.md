# Results directory

Generated JSON, CSV, figures, and logs are written here. Large generated artifacts are ignored by Git.

For paper reproducibility, release:

- the final `results_real.json`;
- a per-seed CSV exported from `track_a_per_seed`;
- the exact Git commit hash;
- the command and environment used for the run.

Do not manually edit generated result files.
